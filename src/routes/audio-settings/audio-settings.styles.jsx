import styled from "styled-components";

export const AudioSettingsContainer = styled.div`
  padding: 24px 32px 0;
  font-size: 24px;
  font-weight: 700;
  line-height: 32px;
  color:var(--color-main-font);
`